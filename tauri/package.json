{
  "name": "adventure-land",
  "version": "1.3.1",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "build:mac": "MAC_APP=\"src-tauri/target/universal-apple-darwin/release/bundle/macos/Adventure Land.app\"; ENTITLEMENTS=\"src-tauri/Entitlements.plist\"; PATH=\"$(dirname \"$(rustup which cargo)\"):$PATH\" RUSTFLAGS=\"--remap-path-prefix=$HOME=.\" tauri build --target universal-apple-darwin && codesign --force --deep --sign - --entitlements \"$ENTITLEMENTS\" \"$MAC_APP\" && codesign --verify --deep --strict \"$MAC_APP\" && ! strings -a \"$MAC_APP/Contents/MacOS/adventure-land\" | grep -Fq \"$HOME\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
